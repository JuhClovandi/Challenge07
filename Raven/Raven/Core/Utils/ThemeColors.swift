import SwiftUI

// Centraliza as cores do tema para fácil manutenção.
struct ThemeColors {
    static let background = Color(red: 0.4, green: 0.1, blue: 0.1)
    static let cardBackground = Color(red: 0.5, green: 0.15, blue: 0.15)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.8)
    static let buttonBackground = Color(red: 0.3, green: 0.05, blue: 0.05)
}
